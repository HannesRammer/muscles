<%= form_with(model: user_to_exercise, local: true) do |form| %>
  <% if user_to_exercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_to_exercise.errors.count, "error") %> prohibited this user_to_exercise from being
        saved:</h2>

      <ul>
        <% user_to_exercise.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :user_to_exercise_user_id %>
  </div>

  <div class="field">
    <%= form.label :exercise_id %>
    <%= form.number_field :exercise_id, id: :user_to_exercise_exercise_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
